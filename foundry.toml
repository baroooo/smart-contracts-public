[profile.default]
src = "src"
out = "out"
libs = ["node_modules"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
via_ir = true

# Remappings to match the OpenZeppelin imports
remappings = [
    "@openzeppelin/=node_modules/@openzeppelin/"
]
